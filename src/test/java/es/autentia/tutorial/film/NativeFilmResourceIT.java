package es.autentia.tutorial.film;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeFilmResourceIT extends FilmResourceTest {

    // Execute the same tests but in native mode.
}